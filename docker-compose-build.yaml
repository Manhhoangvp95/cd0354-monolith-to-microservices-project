version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: muthiendia/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: muthiendia/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: muthiendia/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: muthiendia/udagram-frontend:local
